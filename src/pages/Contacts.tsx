import ContactPage from '../components/ContactPage/ContactPage'

const Contacts = () => {
    return (
        <div className='page'>
            <ContactPage />
        </div>
    );
};

export default Contacts;